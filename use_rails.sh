# docker-compose run --rm web rails runner 'Joke.create(body: "Knock! Knock! Whos there? Owls say. Owls say who? Yes, they do.")'

# docker-compose run --rm web rails db:create db:migrate
# run all new migrations: docker-compose run --rm web rails db:migrate

# docker-compose run --rm web rails generate scaffold post title:string body:text
